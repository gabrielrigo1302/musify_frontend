const blackDefault = '#191414';
const blackBlurredDefault = 'rgba(0,0,0, 0.8)';
const greenDefault = '#1DB954';
const whiteDefault = '#FFF';

const colors = {
    blackDefault,
    blackBlurredDefault,
    greenDefault,
    whiteDefault
};

export default colors;
